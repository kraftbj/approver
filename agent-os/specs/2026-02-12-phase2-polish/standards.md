# Standards

- Follow existing Bubble Tea message/command patterns
- All new messages in messages.go
- Runtime flags on PR struct (json:"-") for UI state
- Existing test patterns: test command construction, not execution
- Keep the single-model architecture (all state in home struct)
